# Movie Collection Manager

A Spring Boot application for managing your movie collection.

## Prerequisites
- Java 17+
- Maven

## How to run
You can use the Maven wrapper included in the project to run the application:

```bash
./mvnw spring-boot:run
```

Or on Windows:

```cmd
mvnw.cmd spring-boot:run
```
